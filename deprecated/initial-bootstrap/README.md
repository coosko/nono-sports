This directory archives the initial bootstrap implementation that existed
before the project architecture was defined.

The code here is kept for historical reference only and is not part of the
active implementation.
